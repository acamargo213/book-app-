<div class="media">
  <div class="media-left">
    <a href="#"
      <img class="media-object product-main" src="<%= book.picture_url %>" alt="...">
    </a>
  </div>
  <div class ="media-body">
    <h1 class= "media-heading"><%= @book.name %></h1>
    <%= @product.description %>
    <hr>
    <h4>Price: $<%= book.price %></h4>
    <%= link_to "Edit", '/books/#{product.id}/edit', class: 'btn btn-default' %>
  </div>
</div>